# Copy this file to ".env.local" (same folder) and fill in your real values.
# ".env.local" is git-ignored, so your anon key never gets committed.
#
# run.ps1 reads these values. Lines starting with # are ignored.
# Do NOT wrap values in quotes or angle brackets.
#
# Password-reset deep links also require this URL in Supabase Dashboard:
#   Authentication → URL Configuration → Redirect URLs
#   rw.entos.smeos://login-callback/

SUPABASE_URL=https://jurhggehykboqpjfigbt.supabase.co
SUPABASE_ANON_KEY=PASTE_YOUR_REAL_ANON_KEY_HERE
