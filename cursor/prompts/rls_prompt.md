# Cursor Prompt: Row-Level Security

Help me create Supabase Row-Level Security policies for SME-OS.

Rules:

- Users must only access rows for tenants where they have active membership in user_tenants.

- Platform admin access must be separate.

- Do not allow cross-tenant access.

- Explain each policy simply.

- Include SELECT, INSERT, UPDATE, and DELETE policies only where needed.

- Prefer soft delete over hard delete.